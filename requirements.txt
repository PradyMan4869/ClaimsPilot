# Agents / protocol
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.6

# LLM (LM Studio speaks the OpenAI API)
openai>=1.30

# Multi-agent framework (used inside the Validator agent)
crewai>=0.80

# MCP + storage
mcp>=1.0
pymongo>=4.6

# Observability
langsmith>=0.1.80

# UI + documents
gradio>=4.36
pypdf>=4.2

# Config / tests
python-dotenv>=1.0
pytest>=7.4
pytest-asyncio>=0.23
